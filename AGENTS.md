# 調唱專案協作規範

所有工程師與 AI 協作者都必須遵循 [docs/DEVELOPMENT_PROTOCOL.md](docs/DEVELOPMENT_PROTOCOL.md)。

## 核心原則：極簡 Token 與先靶後箭

1. **既有功能檢驗（標準化程式驗收）**：
   - 凡未變更的既有功能，一律優先執行**標準化檢驗程序**（如 `pnpm run check`），由程式輸出結構化 LOG 作為客觀證明。
   - 嚴禁透過重複閱讀大量 MD 文件逐條推敲既有功能，以程式 LOG 取代人工與文字反覆推論，大幅節省 Token 並杜絕假報告。

2. **新功能開發（先立檢驗標靶，再寫程式碼）**：
   - **步驟一（立靶）**：在撰寫任何程式碼前，**必須先定義好這項新功能的「檢驗方式」與「預期結果」**（如新增單元測試案例、端到端斷言或可量化指標）。
   - **步驟二（射箭）**：依據目標進行最小必要範圍的程式碼撰寫與編輯。
   - **步驟三（驗收）**：使用步驟一所預先設定的檢驗方式執行驗收，驗證是否精準命中標靶，**嚴禁先寫程式碼再事後拼湊標準（先射箭再畫靶）**。

---

### 開始工作前：
1. 檢視目前 `package.json` 版本與 `public/manifest.json` 是否一致。
2. 若為既有功能回歸，直接執行標準化檢驗腳本確認基準狀態。
3. 若為新功能，先定義檢驗腳本／案例與成功指標。

### 完成工作前：
1. 不在程式碼、測試、文件或發布包放入任何 API Key 或個人資料。
2. 不用隨機值或假成功訊息冒充演算法。
3. 執行標準化檢驗程序（型別檢查、單元測試、正式建置與端到端驗收）確認全部 PASS。
4. 簡明記錄變更檔案、驗證命令、LOG 結果、未完成風險與下一步。
